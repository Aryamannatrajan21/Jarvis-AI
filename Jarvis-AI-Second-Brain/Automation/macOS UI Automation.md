# macOS UI Automation

macOS enforces strict sandboxing and security limits. In order for JARVIS to control macOS applications, the user must explicitly grant the executing Terminal "Accessibility" and "Automation" permissions.

JARVIS leverages the [[AppleScript]] language to interact with the OS. He has specific hard-coded logic to elegantly bypass difficult UI limitations, such as his deep [[Spotify Intelligence]].
